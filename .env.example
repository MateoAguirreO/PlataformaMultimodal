# Copiar a .env (NO se commitea, ya esta en .gitignore) y llenar la key real.
# Sin esto, /explain sigue funcionando pero sin el paso de verificacion jr/senior
# (health devuelve reflexion_disponible: false y /explain avisa por que).
GEMINI_API_KEY=
